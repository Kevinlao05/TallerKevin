function App() {
  return (
    <div style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      height: "100vh",
      backgroundColor: "#282c34",
      color: "white",
      fontSize: "2rem",
      fontFamily: "Arial, sans-serif"
    }}>
      React with Netlify CI CD pipeline
    </div>
  );
}

export default App;
